selfhealing:
  target: prod
  outputs:
    prod:
      type:      snowflake
      database:  SELFHEALING_PROD
      schema:    PUBLIC
      role:      SELFHEALING_PIPELINE
      warehouse: SELFHEALING_WH
